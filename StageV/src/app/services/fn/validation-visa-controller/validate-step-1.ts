/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VisaRequest } from '../../models/visa-request';

export interface ValidateStep1$Params {
  id: number;
  approved: boolean;
}

export function validateStep1(http: HttpClient, rootUrl: string, params: ValidateStep1$Params, context?: HttpContext): Observable<StrictHttpResponse<VisaRequest>> {
  const rb = new RequestBuilder(rootUrl, validateStep1.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('approved', params.approved, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VisaRequest>;
    })
  );
}

validateStep1.PATH = '/api/validation-visas/step1/{id}';
