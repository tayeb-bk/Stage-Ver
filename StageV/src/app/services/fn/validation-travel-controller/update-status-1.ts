/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TravelRequest } from '../../models/travel-request';

export interface UpdateStatus1$Params {
  id: number;
  status: string;
}

export function updateStatus1(http: HttpClient, rootUrl: string, params: UpdateStatus1$Params, context?: HttpContext): Observable<StrictHttpResponse<TravelRequest>> {
  const rb = new RequestBuilder(rootUrl, updateStatus1.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('status', params.status, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TravelRequest>;
    })
  );
}

updateStatus1.PATH = '/api/validation-travels/{id}/status';
